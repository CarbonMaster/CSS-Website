import styled from "@emotion/styled";
import { MainSection, Menu } from "../components";

const All = styled.div``;

export const Index: React.FC = () => {
  return (
    <All>
      {/* <Menu /> */}
      <MainSection />
    </All>
  );
};

export default Index;
